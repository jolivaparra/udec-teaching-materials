\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{udec-pset}

% ========== PAQUETES NÚCLEO ==========
\RequirePackage{etoolbox} % Para condicionales lógicos
\RequirePackage[T1]{fontenc}
\RequirePackage[spanish,es-tabla]{babel}

% Matemáticas y Ciencia Base
\let\Bbbk\relax % Previene conflicto entre amsmath y newtxmath
\RequirePackage{mathtools}
\RequirePackage{amssymb}
\RequirePackage{siunitx}
\RequirePackage{esvect}
\RequirePackage{bm}
\RequirePackage{tikz}

% ========== CONFIGURACIÓN SIUNITX ==========
\sisetup{per-mode = symbol, output-decimal-marker = {.}}

% Extras Documento y Estética Estructural
\RequirePackage{graphicx}
\RequirePackage[most]{tcolorbox}
\RequirePackage[shortlabels]{enumitem}
\RequirePackage{float}
\RequirePackage{subfig} % Múltiples figuras
\RequirePackage{comment} % Gestor para ocultar/mostrar soluciones
\RequirePackage{multicol} % Múltiples columnas

\setlength{\jot}{7pt}

% Tipografía e Interlineado
\RequirePackage{anyfontsize}
\RequirePackage{newtxtext, newtxmath}
\RequirePackage{setspace}
\onehalfspacing

% Geometría y Enlaces
\RequirePackage[a4paper, left=2cm, right=2cm, top=1.5cm, bottom=2.5cm, headheight=40pt, includehead]{geometry}
\RequirePackage[hidelinks, pdfusetitle, linktoc=all]{hyperref}

% ========== COLORES Y VARIABLES GLOBALES ==========
\definecolor{UdeCPrincipal}{HTML}{223c6a}
\definecolor{UdeCSecundario}{HTML}{ffffff}
\definecolor{UdeCAcento}{HTML}{e69b0a}
\definecolor{UdeCAlerta}{HTML}{E74C3C}

% Valores por defecto
\newcommand{\nombreAsignatura}{Nombre Asignatura}
\newcommand{\codigoAsignatura}{000000}
\newcommand{\infoSemestre}{20XX - X}
\newcommand{\nombreAyudante}{Nombre Ayudante}
\newcommand{\contactoAyudante}{correo@udec.cl}

\newcommand{\tipoDoc}{Guía de Problemas}
\newcommand{\numeroDoc}{00}
\newcommand{\temaDoc}{Tema General}

% Macros de configuración de variables
\newcommand{\asignatura}[2]{\renewcommand{\nombreAsignatura}{#1}\renewcommand{\codigoAsignatura}{#2}}
\newcommand{\semestre}[1]{\renewcommand{\infoSemestre}{#1}}
\newcommand{\ayudante}[2]{\renewcommand{\nombreAyudante}{#1}\renewcommand{\contactoAyudante}{#2}}

% Comando para el documento
\newcommand{\doc}[3][Guía de Problemas]{%
    \renewcommand{\tipoDoc}{#1}%
    \renewcommand{\numeroDoc}{#2}%
    \renewcommand{\temaDoc}{#3}%
    \setcounter{problemaNum}{0}%
}

% Importación silenciosa del archivo de configuración
\IfFileExists{../infoAsignatura.tex}{\input{../infoAsignatura.tex}}{}

% Macro extra
\newcommand{\fasor}[2]{#1\angle #2^{\circ}}

% ========== ENCABEZADO Y PIE DE PÁGINA ==========
\RequirePackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\lhead{\normalsize \scshape Universidad de Concepción \\ Facultad de Ingeniería \\ Depto. de Ingeniería Eléctrica}
\chead{{\bfseries \codigoAsignatura\ -- \nombreAsignatura} \\ \infoSemestre}
\rhead{\includegraphics[height=2.5\baselineskip]{logo-header.png}}
\lfoot{\footnotesize \scshape \nombreAyudante}
\cfoot{\small \thepage}
\rfoot{\footnotesize \contactoAyudante}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}

\newcommand{\crearEncabezado}{
    \begin{center}
        {\LARGE \tipoDoc \ifx\numeroDoc\empty\else\ N$^\circ$\numeroDoc\fi} \\[0.2cm]
        {\large \temaDoc} \\[0.4cm]
    \end{center}
}

% ========== BLOQUES DE CONTENIDO (Cajas) ==========
\tcbset{
    baseBox/.style={
            enhanced, breakable, colback=white, arc=1mm, boxrule=0.6pt, leftrule=4pt,
            fonttitle=\bfseries\normalsize, before skip=1.5em, after skip=1.5em,
            sharp corners=downhill, attach boxed title to top left={xshift=4mm, yshift=-3mm},
            coltitle=white, top=10pt, bottom=4pt, left=8pt, right=8pt, #1
        }
}
\newtcolorbox{boxTeoria}[2][]{baseBox={#1}, colframe=UdeCPrincipal, title={#2}, boxed title style={colback=UdeCPrincipal, sharp corners, rounded corners=northeast, arc=3mm}}
\newtcolorbox{boxAlerta}[2][]{baseBox={#1}, colframe=UdeCAlerta, title={#2}, boxed title style={colback=UdeCAlerta, sharp corners, rounded corners=northeast, arc=3mm}}
\newtcolorbox{boxEjemplo}[2][]{baseBox={#1}, colframe=UdeCAcento, title={#2}, coltitle=black, boxed title style={colback=UdeCAcento, sharp corners, rounded corners=northeast, arc=3mm}}
\newtcolorbox{boxNota}[2][]{baseBox={#1}, colframe=gray!60, title={#2}, boxed title style={colback=gray!60, sharp corners, rounded corners=northeast, arc=3mm}}

% ========== SISTEMA DE PROBLEMAS ==========
\newcounter{problemaNum}

% Numeración inteligente adaptada al número del documento
\renewcommand{\theproblemaNum}{%
    \ifx\numeroDoc\empty \arabic{problemaNum}%
    \else \numeroDoc.\arabic{problemaNum}\fi%
}

% Macro simplificado y libre para los problemas
\newcommand{\problema}[1][]{%
    \par \vspace{1.5em}%
    \refstepcounter{problemaNum}%
    \noindent\ignorespaces%
    {\bfseries Problema \theproblemaNum}%
    \ifstrempty{#1}%
    {\textbf{.}}%
    { \textbf{[#1].}}%
    \enskip\ignorespaces% Mantiene el texto en la misma línea
}

% ========== SISTEMA DE SOLUCIONES ==========
\newif\if@soluciones
\newcommand{\mostrarSoluciones}{\@solucionestrue}

\newtcolorbox{CajaSolucionDef}{
    enhanced, breakable, frame hidden, boxrule=0pt, colback=white,
    borderline west={3pt}{0pt}{UdeCAcento}, left=12pt, right=0pt, top=5pt, bottom=5pt,
    parbox=false,
    before upper={
            \noindent{\large\bfseries\color{UdeCPrincipal}Solución \theproblemaNum.}\par\vspace{0.5em}
            \setlength{\parindent}{15pt}
        }
}

\AtBeginDocument{
    \if@soluciones
        \newenvironment{solucion}{%
            \setcounter{equation}{0}%
            \renewcommand{\theequation}{\theproblemaNum.\arabic{equation}}%
            \begin{CajaSolucionDef}%
                }{%
            \end{CajaSolucionDef}%
            \vspace{5pt}%
        }
    \else
        \excludecomment{solucion}
    \fi
}